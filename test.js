const path = require("path");
const config = require(path.resolve("config", "database.js"));

console.log("====================================");
console.log(config);
console.log("====================================");
